# NEAT
A simple file sorting program that is based off of user-given rulesets.

To sort a certain extension use .*\.txt

To sort what file starts with use a.*
(replace a with whatever a file should start with)

Absolute Path and Relative Path both accepted.